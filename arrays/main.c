/*
 * objective : implement arrays using struct
 */
#include <stdio.h>
struct Array{
    int *pointer_to_arr;
    int arr_size;
    int arr_length;

};
void displayArray(struct Array arr);

int main() {
    
    return 0;
}
